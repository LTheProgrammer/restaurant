import NavBar from "./Nav.";

const Header = () => {
    return (
        <>
            Header
            <nav>
                <NavBar />
            </nav>
        </>
    );
};

export default Header;