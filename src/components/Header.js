import NavBar from "./Nav.";

const Header = () => {
    return (
        <header>
            Header
            <nav>
                <NavBar />
            </nav>
        </header>
    );
};

export default Header;